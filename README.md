Python Text to Speech

# Requirements

This project requires the following libraries:

## Built-in Python Modules

```python
asyncio
```

## External Dependencies

```python
edge-tts
```

## Installation

```bash
pip install edge-tts
```

## Imports Used

```python
import asyncio
import edge_tts
```


Since `asyncio` is built into Python, the only package users actually need to install is:

```bash
pip install edge-tts
